String.valueOf(); = int into String
get.getNumericValue(); = char into int
.charAt() = accessing char in a String
dtype variable = condition? iftrue : iffalse; //String weight = x>200 ? "heavy" : "light";
LCM = (N1 *N2) / GCD
string1.equals()
ArrayList< dtype > Name = new ArrayList<>();
arraylist.get( index);
arraylist.add(element @end)
Switch( value of check){
    case value1:
        content;
        break;
    case value2:
        content;
        break;
    .
    .
    .
    default: //when nothing matches then this will print!
        content;
        break;
}
